<div class="min-h-screen bg-gray-900 text-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <%= link_to "← Back to Templates", agent_templates_path, class: "text-blue-400 hover:text-blue-300 mb-6 inline-block" %>
    
    <div class="bg-gray-800 rounded-lg p-8">
      <div class="flex items-start justify-between mb-6">
        <div class="flex items-center gap-4">
          <div class="text-6xl"><%= @template.icon || "🤖" %></div>
          <div>
            <h1 class="text-3xl font-bold"><%= @template.name %></h1>
            <p class="text-gray-400"><%= @template.role %></p>
          </div>
        </div>
        <% if @template.featured %>
          <span class="px-3 py-1 bg-yellow-600 rounded text-sm">Featured</span>
        <% end %>
      </div>
      
      <div class="mb-6">
        <h2 class="text-lg font-semibold mb-2">Description</h2>
        <p class="text-gray-300"><%= @template.description %></p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
        <div>
          <h3 class="text-sm font-semibold text-gray-400 mb-1">Category</h3>
          <p class="text-white"><%= @template.category.titleize %></p>
        </div>
        <div>
          <h3 class="text-sm font-semibold text-gray-400 mb-1">Version</h3>
          <p class="text-white"><%= @template.version %></p>
        </div>
        <% if @template.author.present? %>
          <div>
            <h3 class="text-sm font-semibold text-gray-400 mb-1">Author</h3>
            <p class="text-white"><%= @template.author %></p>
          </div>
        <% end %>
      </div>
      
      <% if @template.system_prompt.present? %>
        <div class="mb-6">
          <h2 class="text-lg font-semibold mb-2">System Prompt</h2>
          <pre class="bg-gray-900 p-4 rounded text-sm text-gray-300 overflow-x-auto"><%= @template.system_prompt %></pre>
        </div>
      <% end %>
      
      <% if @template.soul_md.present? %>
        <div class="mb-6">
          <h2 class="text-lg font-semibold mb-2">SOUL.md Preview</h2>
          <pre class="bg-gray-900 p-4 rounded text-sm text-gray-300 overflow-x-auto max-h-64"><%= @template.soul_md %></pre>
        </div>
      <% end %>
      
      <!-- Deploy Form -->
      <%= form_with url: deploy_agent_template_path(@template), method: :post, class: "space-y-4" do |f| %>
        <div>
          <%= f.label :name, "Agent Name (optional)", class: "block text-sm font-medium mb-2" %>
          <%= f.text_field :name, placeholder: @template.name, 
              class: "w-full px-4 py-2 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
          <p class="text-xs text-gray-500 mt-1">Leave blank to use template name</p>
        </div>
        
        <%= f.submit "Deploy Agent", 
            class: "w-full px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-lg font-semibold cursor-pointer" %>
      <% end %>
    </div>
  </div>
</div>
